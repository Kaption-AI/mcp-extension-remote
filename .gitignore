node_modules/
dist/
.wrangler/
.dev.vars
.next/
.open-next/
next-env.d.ts
tsconfig.tsbuildinfo

# Generated by scripts/build-config.mjs from wrangler.template.jsonc.
# Keeping this out of git is the deploy guard — without a wrangler.jsonc,
# `wrangler deploy` aborts. CI runs `build:config` before deploy; local
# devs use `npm run dev` which generates a dev-only stub.
wrangler.jsonc
